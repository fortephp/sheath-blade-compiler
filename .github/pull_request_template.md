<!--
Thanks for sending this. A short description of what changes and why is
usually enough; the checklist below matches the checks run by CI.
-->

## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## Checklist

- [ ] `vendor/bin/pest` passes
- [ ] `vendor/bin/phpstan analyse` passes
- [ ] `vendor/bin/pint --test` and `vendor/bin/rector process --dry-run` are clean
- [ ] Tests cover any changed Blade syntax, compiler integration, or diagnostic mapping
- [ ] User-facing behavior or configuration changes are documented
